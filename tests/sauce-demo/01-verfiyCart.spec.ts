import { test as base } from '../../fixture/saucedemo-login';
import {test} from '@playwright/test';

test('verify cart functionality after login', async ({ page }) => {
  // Use the login fixture to log in
  

  // Add your test steps to verify the cart functionality
  // For example:
//   await page.click('.inventory_item:first-child .btn_inventory');
//   await page.click('.shopping_cart_link');
  
//   // Add assertions to verify the cart functionality
//   const cartItemCount = await page.textContent('.cart_quantity');
//   test.expect(cartItemCount).toBe('1');
});